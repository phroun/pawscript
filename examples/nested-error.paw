# Test error reporting in nested contexts

echo "Testing nested parse errors"

# Brace expression containing leading operator
echo "Nested: {& echo this fails in brace}"

# Macro containing brace with error
macro nested_error, {
  echo "In macro: {& echo nested failure}"
}

call nested_error
