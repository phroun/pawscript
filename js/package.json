{
  "name": "pawscript",
  "description": "PawScript runtime (Go → WASM) with JS wrapper — Node + Browser",
  "version": "2.0.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "vm.wasm",
    "wasm_exec.js"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build:ts": "tsc -p tsconfig.json && npm run copy:wasm",
    "copy:wasm": "mkdir -p dist && cp src/wasm_exec_loader.js dist/ || true && cp ../pawscript.wasm dist/ || true",
    "build": "npm run clean && npm run build:ts"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.2.2"
  }
}
