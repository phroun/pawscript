// src/wasm_exec_loader.d.ts
// Type declarations for wasm_exec_loader.js (non-module script)

declare module './wasm_exec_loader.js' {
  // This file is a plain script that sets up the global Go object
  // It doesn't export anything as an ES module
  // Using empty export to satisfy TypeScript
  export {};
}
