# hello.paw - Simple greeting script

echo "Hello from PawScript!"
echo "Arguments provided:"
echo "Count: {argc}"

macro show_arg (
  echo "Arg $1: {argv $1}"
);

# Show first few arguments if they exist;
argc & show_arg 0;
argc & show_arg 1;
argc & show_arg 2;
